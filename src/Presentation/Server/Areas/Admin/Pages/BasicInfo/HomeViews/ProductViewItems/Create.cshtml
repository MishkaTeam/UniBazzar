@page "{homeViewId:guid}"
@model Server.Areas.Admin.Pages.BasicInfo.HomeViews.ProductViewItems.CreateModel

@{
    var pageTitle =
        $"{Resources.DataDictionary.CreateOf} {Resources.DataDictionary.ProductItem}";

    ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;
}


<form method="post" enctype="multipart/form-data">

    <section-form>

        <fieldset>

            <section-form-header>
                @(pageTitle)
            </section-form-header>

            <partial name="PartialViews/_DisplayPageMessages" />

            <input hidden asp-for="CreateViewModel.HomeViewId" />

            <div class="mb-3">
                <label asp-for="CreateViewModel.ProductId" class="form-label"></label>
                <select asp-for="CreateViewModel.ProductId" asp-items="Model.ProductList" class="form-select"></select>
                <span asp-validation-for="CreateViewModel.ProductId" class="field-validation-valid text-danger"></span>
            </div>

            <ub-full-input asp-for="CreateViewModel.Ordering" />

        </fieldset>

        <section-form-buttons>
            <button-create />
            <button-reset />
        </section-form-buttons>

    </section-form>

</form>
