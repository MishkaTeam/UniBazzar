@page "{id:guid}"
@model Server.Areas.Admin.Pages.BasicInfo.Products.ProductPriceLists.IndexModel
@{
    var pageTitle =
        Resources.DataDictionary.ProductPriceList;

    ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;
}


<div class="row justify-content-between px-2 my-2">
    <a class="col-auto btn btn-primary" asp-page="../Index" asp-area="Admin">@(Resources.ButtonCaptions.Back)</a>
    <h5 class="col-auto">@(Resources.DataDictionary.FeaturesOf) "@(Model.ProductViewModel.Name)"</h5>
    <a class="col-auto btn btn-primary" asp-page="Create" asp-route-Id="@(Request.RouteValues["id"])" asp-area="Admin">@(Resources.ButtonCaptions.Create)</a>
</div>

<table class="table table-hover">
    <thead>
        <tr>
            @(Html.Ub_DisplayStringWithTh(Resources.DataDictionary.Price))
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var productprice in Model.ViewModel)
        {
            <tr>
                @(Html.Ub_DisplayStringWithTd(productprice.Price.ToString()))
                <td>
                    <div class="btn-group" role="group" aria-label="Basic example">
                        <a asp-page="Update" asp-route-Id="@(productprice.Id)" type="button" class="btn btn-warning">@(Resources.ButtonCaptions.Edit)</a>
                        <a asp-page="Delete" asp-route-Id="@(productprice.Id)" type="button" class="btn btn-danger">@(Resources.ButtonCaptions.Delete)</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>