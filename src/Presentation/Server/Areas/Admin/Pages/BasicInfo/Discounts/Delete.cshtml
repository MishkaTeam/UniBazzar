@page "{id}"
@model Server.Areas.Admin.Pages.BasicInfo.Discounts.DeleteModel
@{
	var pageTitle =
	$"{Resources.DataDictionary.DeleteOf} {Resources.DataDictionary.DiscountCode}";

	ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;
}

<form method="post">

	<section-form>
		<fieldset disabled>
			<section-form-header>
				@(pageTitle)
			</section-form-header>

			<partial name="PartialViews/_DisplayPageMessages" />
			<input hidden asp-for="ViewModel.Id" dir="ltr" />

			<ub-full-input asp-for="ViewModel.Title" />

			<ub-full-input asp-for="ViewModel.DiscountCode" />

			<ub-full-checkbox asp-for="ViewModel.IsActive" />

		</fieldset>

		<section-form-buttons>
			<button-delete />
		</section-form-buttons>

	</section-form>

</form>
